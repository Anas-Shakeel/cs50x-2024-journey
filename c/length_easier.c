// Lecture 02 - "Arrays"

#include "cs50.c"
#include <stdio.h>
#include <string.h>

int main(void)
{
    string name = "Anas";

    // Find the length of 'name' string
    int len = strlen(name);
    // Print the length
    printf("Length: %i\n", len);
}